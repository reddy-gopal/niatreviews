# Backend URL. Leave empty for same-port mode (Next.js proxies /api and /media to Django).
# Use empty when running with scripts/run-unified.mjs and ngrok.
# NEXT_PUBLIC_API_BASE_URL=
NEXT_PUBLIC_API_BASE_URL=
